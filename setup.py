from setuptools import setup, find_packages

setup(name='toon',
      version='1.0.0',
      description='Toon from rvdm',
      keywords='toon',
      author='Robert van der Meulen',
      url='https://github.com/rvdm/toon',
      zip_safe=False,
      platforms=["any"],
      packages=find_packages(),
      )
